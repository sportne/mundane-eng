# Requirement output fixtures

These serialized YAML 0.3 artifacts exercise values, provenance and strict consumers.
`consume.py` checks their published shape independently from the source parser.
See [output 0.1](../../0012-requirement-semantic-output-0.1.md).
