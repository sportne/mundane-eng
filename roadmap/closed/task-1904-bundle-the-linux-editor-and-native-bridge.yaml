format: mundane-work-yaml-0.2
id: TC-1904
kind: task
title: Bundle the Linux Editor and Native Bridge
status: Complete
dependencies:
- TC-1902
- TC-1903
relations: []
planning:
  stage: '19'
  type: Packaging
  condition: ''
  unlocks: ''
  statusNote: ''
body: |2

  ## Problem and outcome

  Users must build and locate a matching bridge separately from the VSIX. Assemble a local Linux x86-64 bundle with matching VSIX/bridge, licenses, metadata, checksums and installation instructions.

  ## Scope and acceptance

  Archive contents and hashes verify; assembly from identical inputs is repeatable; unsupported platform or inconsistent versions fail without publishing a bundle.

  ## Evidence

  Record actual owning checks and changed artifacts before completion. Use repository
  examples and repeatable local tests; do not infer hosted or cross-platform results.

  ## Dependencies and boundaries

  Predecessor decisions establish the contracts used here. Tasks 1902 and 1903
  unlock packaging, which unlocks installed-package checks. Human-authored YAML
  and IDs remain authoritative. No new artifact language, Marketplace publication,
  release milestone or unsupported platform promise is included.

  ## Compatibility, risks and stop conditions

  Preserve requirement source and compiled semantics. Keep wire compatibility explicit.
  Stop or revise if measurements do not justify an optimization, cached results can
  become stale, or a package needs unverified platform assumptions. Components are
  editor code/tests, version generation, Makefile, distribution and planning guides.

  ## References

  - [Roadmap](../0001-initial-roadmap.md)
  - [Editor contract](../../specification/0024-vscode-editor-0.1.md)

  ## Completion evidence

  The 0.1.1 Linux x86-64 bundle was assembled from matching native and VSIX metadata. Checks passed for complete checksums/inventory, executable mode, runtime notices, glibc symbol ceiling and byte-identical reassembly from identical inputs. Corrupt archives, mismatched VSIX metadata and unsupported platforms were rejected. The installed-runtime checks follow in TC-1905.
