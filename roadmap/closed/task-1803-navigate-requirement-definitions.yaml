format: mundane-work-yaml-0.2
id: TC-1803
kind: task
title: Navigate Requirement Definitions
status: Complete
dependencies: [TC-1802]
relations: []
planning: {stage: '18', type: Implementation, condition: '', unlocks: '', 
    statusNote: ''}
body: |2

  ## Outcome

  Go to a referenced requirement definition across selected YAML files.

  ## Work and acceptance evidence

  Use current parser origins and UTF-16 editor positions; include unsaved target edits and duplicate/missing-target behavior. Do not search prose or select an ambiguous definition. Run actual provider tests.

  ## Compatibility and boundaries

  Human-authored source remains authoritative. Requirements and other artifact
  languages keep independent contracts. No work-item authoring integration, remote
  publishing, automated approval or general language-server framework is included.
  Each implemented behavior needs reproducible tests and contributor documentation.

  ## Completion decision

  Stop or revise if the feature requires hidden project discovery, a competing
  semantic validator, stale edits, unsupported source inference or unbounded input.
  The predecessor's contract must resolve dependent implementation choices first.

  ## References

  - [Roadmap](../0001-initial-roadmap.md)
  - [Source rules](../../specification/0010-requirements-yaml-0.3.md)

  ## Completion evidence

  Actual VS Code definition-provider tests passed for cross-file decomposition, unsaved target line movement, non-BMP UTF-16 target spans, missing and duplicate IDs, and prose exclusion. Maintained Java tests and the native bridge build passed. The editor contract records that invalid snapshots provide no guessed navigation.
